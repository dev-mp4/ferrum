# Ferrum

This language is on very early stage of development but you can still try it!  
Install `cmake` and run `cmake -S . -B build` to bootstrap project.  
Run `cmake --build build` to compile an executable.

## Usage

Run `ferrum <filename>` to compile a single file.